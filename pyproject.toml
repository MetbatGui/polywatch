[project]
name = "polywatch"
version = "0.1.0"
description = "Polymarket macro market insider detection & monitoring"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests",
]
